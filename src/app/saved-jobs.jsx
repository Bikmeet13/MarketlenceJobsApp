import { Text, View } from "react-native";

export default function SavedJobs() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text style={{ fontSize: 24 }}>Saved Jobs Screen</Text>
    </View>
  );
}